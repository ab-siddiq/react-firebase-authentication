/**
 * Steps to use firebase
 * 1. Create a project on console.firebase.google.com 
 * 2. Install firebase
 * 3. Register Web App in firebase
 * 4. copy firebase init with config from firebase project setting into a file 
 * 5. export default app
 * 6. import app firebase.init.js into your app js
 * 7.import getAuth from firebase/auth and create const auth = getAuth(app)
 * 8. turn on google authentication (firebase > authentication > enable google sign in)
 * 9. create google provider
 * 10. signing with popup
 */